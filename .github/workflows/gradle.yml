name: Java CI with Gradle

on:
  push:
     branches: 
     - main
  pull_request:
     branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # 1. Clones your repository on the virtual machine.
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Set up the JDK. Your project uses Java 21.
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      # 3. Add the necessary permissions for the build.
      - name: Grant execute permission for gradlew
        env:
          API_SECURITY_JWT_SECRET_KEY: ${{ secrets.API_SECURITY_JWT_SECRET_KEY }}
          API_SECURITY_KEY: ${{ secrets.API_SECURITY_KEY }}
        run: chmod +x ./gradlew

      # 4. Cache Gradle packages
      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
          restore-keys: ${{ runner.os }}-gradle

      # 5. Build, test, coverage & SonarCloud analysis in a single step
      - name: Build, coverage and analyze
        env:
          API_SECURITY_JWT_SECRET_KEY: ${{ secrets.API_SECURITY_JWT_SECRET_KEY }}
          API_SECURITY_KEY: ${{ secrets.API_SECURITY_KEY }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./gradlew build jacocoTestReport sonar \
            -Dsonar.coverage.jacoco.xmlReportPaths=build/reports/jacoco/test/jacocoTestReport.xml \
            -Dsonar.organization=unq-dapp-grupom
